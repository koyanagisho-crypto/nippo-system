# 運行日報電子入力システム v6 変更履歴（2026-08-15）

引き継ぎ元: Google Docs「運行日報電子入力システム 開発引き継ぎメモ」（v5, 2026-07-12）
ベース: driver_report_system_5.html（= _5_1.html と同一、ハッシュ確認済み）
成果物: driver_report_system.html（本リポジトリ）／配布用コピー driver_report_system_6.html

## 追加修正依頼（小柳さん 2026-08-15）への対応

1. **①② 車両マスタ**: 管理画面に「車両管理」パネルを新設。車両番号⇔車種の紐づけを localStorage（`drs_vehicles`）で管理し、追加・車種変更・車番修正・削除が可能。ドライバー画面の車両番号は従来どおり手打ち（datalistで候補表示）で、マスタに一致すると車種が自動セットされる（`onVehInput`）。乗務員選択による車両自動セット時も連動。
2. **③ 配送先の詳細入力は日通のみ**: `buildS2` を全面書き換え。日通選択時のみ発地/着地/荷主名・品名/件数/着時刻の詳細入力。他5社は「配送先名＋着時刻」の簡易入力に統一。
3. **④ 管理情報の削除**: 日通の「管理情報」カード（管理課所コード/請負階層/上乗手）を削除。
4. **⑤ ガソリン代・給与系の削除**: ガソリン代(gas_fee)、基本料金/待機料/時間割増/助手料金、名鉄の報償費・ガソリン欄をすべて削除。ダッシュボード・集計・帳票からも除去（給油量合計に置換）。
5. **⑥ 給油量＋領収書写真**: 給油量(L)は入力継続。領収書写真の添付を追加（カメラ起動対応、canvasで最大1000px/JPEG70%に圧縮して `gas_receipt` にbase64保存）。管理画面の詳細でサムネイル表示。
6. **⑦ フォルダ管理**: 管理画面に「フォルダ管理」パネルを新設（`drs_folders`）。作成・名称変更・削除、日報の振り分け（詳細画面のプルダウン）、フォルダ別表示・件数表示。フォルダ削除時は中の日報を未分類へ移動。
7. **⑧ 承認不要・編集可能**: 承認/差し戻しワークフローを廃止（approveReport/reject系を削除）。管理者が日報を直接編集できる編集フォームを追加（`openEdit`/`saveEdit`、走行距離は自動再計算、最終編集日時を記録）。**PDF出力はステータスに関係なくいつでも可能**。帳票上の「承認済」スタンプ・承認者表記も削除。
8. **⑨ 未対応/対応済み**: ステータスを `open`（未対応）/`done`（対応済み）の2区分に変更。日報管理画面はタブで切替、ワンクリックでトグル。サイドバーのバッジは未対応件数。既存データは起動時に自動移行（submitted/rejected→open、approved→done）。
9. **⑩ カレンダー改善**: ネイティブのdate inputを廃止し、大きく押しやすいカスタムカレンダーモーダルに変更（月送り・今日/昨日ショートカット・「2026年8月15日（土）」表示）。管理者編集フォームでも同じカレンダーを使用。
10. **⑪ 手書き日報OCR**: 管理画面に「OCR取込」パネルを新設。手書き日報の写真をアップロード→Claude API（`claude-opus-5`、structured outputs、ブラウザ直接アクセス）で読み取り→編集フォームにプリフィル→管理者が確認・修正して「未対応」日報として保存。元画像も日報に添付保存され、詳細画面で確認可能。**利用にはAnthropic APIキーが必要**（OCR画面で入力、ブラウザのlocalStorageにのみ保存）。

## データ構造の変更（report）

- 追加: `gas_receipt`(base64|null), `folderId`(string|null), `ocr`(bool), `ocr_image`(base64|null), `edited_at`
- 変更: `status` は `'open'|'done'`
- 削除: `gas_fee`, `approved_at`, `approved_by`, `reject_comment`

localStorage キー: `drs_reports` / `drs_drivers` / `drs_vehicles`(新) / `drs_folders`(新) / `drs_api_key`(新)

## 検証（2026-08-15 実施）

- JS構文チェックOK、全80関数の存在確認OK、廃止参照（承認系・gas_fee）の残存なし
- ブラウザ実機テスト: ドライバー入力フロー（日通詳細/他社簡易）、送信、車両マスタ連動、カレンダー、管理画面全パネル（日報管理・編集・フォルダ・車両・OCR画面表示）を動作確認
- 6社全帳票のHTML生成をモックwindowで確認（エラーなし、undefined混入なし）

## 留意事項（変更なし・継続）

- 名鉄帳票の方針（運行表への置き換え可否）は引き続き小柳さん確認中 → printMeitetsu は旧実装のまま
- 城北フッターの整備工場名「桧田自動車 048-421-0589」要確認
- 事業計画との整合性ルール、部分置換での修正ルールは継続
- OCRのAPIキーは現状ブラウザ保存（PoC）。Phase 3のクラウド化時はサーバー側に移すこと
